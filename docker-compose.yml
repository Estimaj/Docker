version: '3'

services:
  admin:
    build:
      args:
        user: estima
        uid: 1000
      dockerfile: Dockerfile
      context: ./
    restart: unless-stopped
    volumes:
      - ./Admin:/var/www/html/
    ports:
      - "8101:80"
      - "8448:443"
    extra_hosts:
      - "host.docker.internal:host-gateway"
