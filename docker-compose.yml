version: '3'

services:
  evoapi:
    build:
      args:
        user: estima
        uid: 1000
      dockerfile: Dockerfile
      context: ./
    restart: unless-stopped
    volumes:
      - ./EvoApi:/var/www/html/
    ports:
      - "8103:80"
      - "8446:443"
    extra_hosts:
      - "host.docker.internal:host-gateway"
